# 개발환경용 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 의존성 설치
COPY package.json ./
RUN npm cache clean --force && \
    npm install --legacy-peer-deps

# 소스 코드 복사 (볼륨 마운트로 실시간 반영)
COPY . .

# 개발 서버 포트 노출
EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "run", "dev"] 